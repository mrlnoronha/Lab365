let letra = 0;
while (letra <= 10){
    console.log("letra ... " + letra);
    letra += 1;
    
}

x = 5; // Assign 5 to x

elem = document.getElementById("demo"); // Find an element 
elem.innerHTML = x;           // Display x in the element

var x; // Declare x

x = "Veio do script js"; // Assign 5 to x

elem = document.getElementById("demoa"); // Find an element 
elem.innerHTML = x;           // Display x in the element

var x; // Declare x